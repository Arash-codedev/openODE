####################################################
#  Warning: Autogenerated and subjected to change  #
#  Sun May 27 2014 14:57:49 GMT+1000 (AEST)        #
#  Path: ./CMakeLists.txt                          #
#  Generator mark: G5416829841                     #
####################################################

cmake_minimum_required(VERSION 2.8.9)

project(chaos_act)

find_package(Threads)

include_directories(.)

# Detect operating system
if(${CMAKE_SYSTEM_NAME} STREQUAL "Linux")
    add_definitions(-DSYSTEM_LINUX)
endif()
if(${CMAKE_SYSTEM_NAME} MATCHES "FreeBSD")
    add_definitions(-DSYSTEM_FREEBSD)
endif()
if(${CMAKE_SYSTEM_NAME} MATCHES "Windows")
    add_definitions(-DSYSTEM_WINDOWS)
endif()

# settings
set(Boost_USE_STATIC_LIBS OFF)
set(Boost_USE_MULTITHREADED ON)
set(Boost_USE_STATIC_RUNTIME OFF)
set(BOOST_VERSION "1.65.0")
# Libraries
find_package(Boost ${BOOST_VERSION} COMPONENTS filesystem regex system thread date_time wave)


# Flags
set(CMAKE_CXX_FLAGS         "${CMAKE_CXX_FLAGS} -std=c++17 -Wall -Wconversion -Wfatal-errors -Wextra")
set(CMAKE_CXX_FLAGS_DEBUG   "-Og -g3")
set(CMAKE_CXX_FLAGS_RELEASE "-O3 -DNDEBUG")

include_directories(SYSTEM ${Boost_INCLUDE_DIR})
include_directories(${Boost_INCLUDE_DIRS})

if(NOT Boost_FOUND)
	message(FATAL_ERROR "Boost ${BOOST_VERSION} not found." )
endif()

add_executable(chaos_act
	main.cpp
	auto-coded/types/position.cpp
	hand-coded/systems/actsys/plant_model.cpp
	auto-coded/systems/actsys/manager.cpp
	auto-coded/systems/actsys/solver.cpp
)

# Link
target_link_libraries(chaos_act ${Boost_LIBRARIES})
target_link_libraries(chaos_act ${CMAKE_THREAD_LIBS_INIT})
